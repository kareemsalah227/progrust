services:
  german-tracker:
    build: .
    container_name: german-tracker
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - DATABASE_URL=/app/data/german_tracker.db
      - FRONTEND_DIR=/app/frontend-dist
